import assert from 'assert';
import sinon from 'sinon';

import { FieldView } from './field.view';

describe('FieldView', function() {
    // This space intentionally left blank after view redesign. The view has
    // become more declarative and some of its former functionality has moved
    // into the new FieldValueView subview, which is even more declarative.
    // Tests may be added again when regressions are encountered.
});
